import Link from"next/link";import{getProducts}from"@/lib/queries";import{deleteProduct,updateProductSortOrder}from"../actions";import ProductTable from"@/components/admin/ProductTable";
export default async function ProductsAdmin(){const products=await getProducts({includeDrafts:true});return <><div className="admin-actions"><div><h1>Products</h1><p>Set product order with numbers: 1 appears first, then 2, 3, 4 and 5. You can also manage images, video, datasheet, SEO, AI summary and FAQ.</p></div><Link className="btn-primary" href="/admin/products/new">Add Product</Link></div><ProductTable products={products} deleteAction={deleteProduct} orderAction={updateProductSortOrder}/></>}
